import 'package:flutter/material.dart';

class AppColors {
  // Tons de bleu harmonieux
  static const Color primaryColor = Color(0xFF1E3A8A);      // Bleu foncé (indigo profond)
  static const Color secondaryColor = Color(0xFF2563EB);    // Bleu royal
  static const Color accentColor = Color(0xFF3B82F6);       // Bleu moyen éclatant
  static const Color softBlue = Color(0xFF60A5FA);          // Bleu doux
  static const Color paleBlue = Color(0xFF93C5FD);          // Bleu pâle équilibré
  static const Color lightBlueBackground = Color(0xFFFFFFFF); // Changed from light blue to white
  
  // Neutres complémentaires
  static const Color textColor = Color(0xFF1E293B);         // Bleu-noir (pour textes)
  static const Color greyTextColor = Color(0xFF64748B);     // Gris bleuté
  static const Color whiteColor = Color(0xFFF8FAFC);        // Blanc bleuté
  static const Color blackColor = Color(0xFF0F172A);        // Bleu marine très foncé
  static const Color darkGrey = Color(0xFF334155);          // Bleu-gris foncé
  static const Color darkGrey2 = Color(0xFF1E293B);         // Gris charbon bleuté

  // Dégradés
  static const Color welcomeGradientStart = Color(0xFF1E3A8A); // Indigo
  static const Color welcomeGradientMiddle = Color(0xFF2563EB); // Bleu royal
  static const Color welcomeGradientEnd = Color(0xFF60A5FA);   // Bleu doux

  // Accents doux
  static const Color welcomeBackgroundPattern = Color(0x4060A5FA); // Bleu clair transparent
  static const Color accentViolet = Color(0xFF818CF8);           // Bleu lavande
  static const Color softWhite = Color(0xFFEFF6FF);              // Blanc bleuté très pâle
}